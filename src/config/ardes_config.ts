// AR-DES Security Configuration

export const ARDES_CONFIG = {
    // Obfuscated Admin Path - Change this to something unique
    // For now, using a strong default, but the user can change it.
    ADMIN_PATH: 'ardes-gate-88',

    // Download Token Expiration (in minutes)
    DOWNLOAD_TOKEN_EXPIRY_MIN: 10,

    // Staging Mode (Drafts vs Live)
    STAGING_ENABLED: true,
}
